name: registration_repository
description: Dart package which manages the registration domain.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.3.0  

dev_dependencies:
  flutter_test:
    sdk: flutter
